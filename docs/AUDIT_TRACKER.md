# Supabase Audit Tracker — Clover

**Last updated:** 2026-06-10  
**Source:** `docs/SUPABASE_HANDOFF.md` + `docs/SUPABASE_AUDIT_REVIEW.md`  
**Audited by:** Claude (Codex session)

---

## Phase 1 — Must Fix Before TestFlight

| ID | Finding | Severity | Phase | Owner | Status | Verification Method | Notes |
|---|---|---|---|---|---|---|---|
| A-01 | `profiles.bio` column absent from live schema; TypeScript `Profile` type declared `bio: string \| null`, while the app's canonical About field is `tagline`. | High | 1 | Codex | Fixed | `rg -n "\\.bio\\b|bio:" src` returns no matches. `UserProfileModal` reads only `profile.tagline`, the `Profile` type no longer declares `bio`, and `npx tsc --noEmit` passes. | Fixed 2026-06-08. Removed the `profile.bio` fallback and the TypeScript `bio` field. `tagline` is now the only canonical About field. Supabase schema was intentionally not changed. Manual About-card checks are recorded in `docs/MANUAL_TEST_PROFILE_TAGLINE.md`. |
| A-20 | Plaintext Google OAuth client-secret and Apple `.p8` private-key files were stored locally inside the project workspace. Both were ignored by Git and absent from repository history, but must be treated as exposed because they existed in broadly readable project folders. | High | 1 | Codex | Open | Revoke or rotate the affected Google OAuth client secret and Apple Sign-In key in their provider consoles. Update Supabase with replacement credentials only for providers Clover intentionally supports, then confirm the old credentials no longer authenticate and test Apple sign-in on a physical device. | Both local credential files removed 2026-06-10. Existing `.gitignore` rules cover the filename patterns, and `git log --all` confirmed neither file was committed. External revocation/rotation remains required before TestFlight. |

---

## Phase 2 — During Closed Beta

| ID | Finding | Severity | Phase | Owner | Status | Verification Method | Notes |
|---|---|---|---|---|---|---|---|
| A-02 | `messages` table had two PERMISSIVE INSERT policies. The weaker one ("Users can send messages") checked only `sender_id = auth.uid()` with no match membership check. | High | 2 | Codex | Fixed | Apply `supabase/015_drop_weak_messages_insert_policy.sql`, then run `SELECT policyname, with_check FROM pg_policies WHERE schemaname = 'public' AND tablename = 'messages' AND cmd = 'INSERT';` Confirm only `"Users can insert own messages"` remains and its check includes sender identity plus match membership. | Fixed in repository 2026-06-08 with an idempotent migration that drops only the weaker policy. No app code changed. Live Supabase execution and the policy query remain required deployment verification. |
| A-03 | `friendships` table had an ALL policy ("Users can manage friendships") with no `WITH CHECK`. Either party could directly mutate friendship rows, bypassing the secured RPC contract. | High | 2 | Codex | Fixed | Apply `supabase/016_restrict_friendships_writes.sql`, then run `SELECT policyname, cmd, qual, with_check FROM pg_policies WHERE schemaname = 'public' AND tablename = 'friendships';` Confirm `"Users can manage friendships"` and `"Users can see own friendships"` are absent and `"Users can read own friendships"` is the only direct-table policy. Manually verify send, accept, decline, and unmatch through the app. | Fixed in repository 2026-06-08. The migration removes both dashboard-created policies. No direct INSERT, UPDATE, or DELETE policy is added because all app writes use SECURITY DEFINER RPCs (`send_friend_request`, `respond_to_friend_request`, and `unmatch_user`); direct app table access is read-only. Live Supabase execution and manual flow verification remain required. |

---

## Phase 3 — Before Public Launch

| ID | Finding | Severity | Phase | Owner | Status | Verification Method | Notes |
|---|---|---|---|---|---|---|---|
| A-04 | The live `friendships.status` column used a custom `friendship_status` enum with default `'active'`, diverging from the app's three-state contract (`pending`, `accepted`, `declined`). | Medium | 3 | Codex | Fixed | Apply `supabase/017_normalize_friendship_status.sql`, then query `information_schema.columns` to confirm `data_type = 'text'` and `column_default = 'pending'::text`. Confirm `SELECT DISTINCT status FROM friendships` returns only `pending`, `accepted`, or `declined`, and inspect `friendships_status_check`. Manually verify friend request send, accept, and decline. | Fixed in repository 2026-06-08. The guarded migration aborts if unknown statuses exist, maps legacy `active` rows to `accepted`, converts the column to TEXT, sets the default to `pending`, and restores the three-state CHECK constraint. Depends on A-03 so clients cannot bypass the RPC contract with direct writes. Live Supabase execution and manual flow verification remain required. |
| A-05 | The dashboard-created `invites` table UPDATE policy ("Anyone can claim invite") had no `WITH CHECK`, allowing a caller to assign `claimed_by_user_id` to another UUID. | Medium | 3 | Codex | Fixed | Apply `supabase/018_secure_invite_claim_policy.sql`, then run `SELECT qual, with_check FROM pg_policies WHERE schemaname = 'public' AND tablename = 'invites' AND policyname = 'Anyone can claim invite';` Confirm `qual` requires an unclaimed row and `with_check` requires `claimed_by_user_id = auth.uid()`. | Fixed in repository 2026-06-08. The conditional migration updates the policy only when the dashboard-created table and policy exist, preserving clean-environment compatibility. The table remains unused by app code. Live Supabase execution and an authenticated negative test assigning another UUID remain required. |
| A-06 | `delete_account()` deleted `storage.objects` metadata directly instead of using the Storage API, so avatar files could persist after account deletion. | Medium | 3 | Codex | Fixed | Create a disposable account with multiple photos, record its user ID and avatar URLs, then delete the account in-app. Confirm the UI signs out, `storage.objects` has no avatar rows for the user, and the recorded avatar URLs no longer return the images. Also test a forced Storage failure and confirm account deletion is aborted with an error. | Fixed in app code 2026-06-08. Account deletion now lists and physically removes every file in the authenticated user's `avatars/<user-id>/` folder through the Supabase Storage API before calling `delete_account()`. Cleanup failure aborts account deletion so it cannot silently leave files behind. The existing RPC metadata delete remains as a harmless fallback. |
| A-07 | The broad `profiles` SELECT policy and table grant exposed private columns including email, phone number, full birthday, precise location, and push tokens to every authenticated user. | Low | 3 | Codex | Fixed | Apply `supabase/019_limit_profile_data_exposure.sql`. As User A, verify direct SELECTs of User B's `email`, `phone_number`, `birthday`, `location`, and `expo_push_token` fail. Verify `public_profiles` returns public fields plus computed age, `get_profile(A)` returns A's private fields, and `get_profile(B)` omits them. Manually test login, onboarding completion, profile editing, discovery, profile modals, friend search by username/email/phone, invites, and chat/group member profiles. | Fixed in repository 2026-06-08. The migration revokes broad profile SELECT access, grants only public columns, adds a curated `public_profiles` view with computed age instead of birth date, adds an own-aware profile RPC, and adds private-field search that returns only public identity fields. App queries were updated accordingly. Live migration and full manual flow verification remain required. |
| A-08 | Auth provider configuration is only partially verified. Email, Apple, and Google are enabled, but Clover currently implements login UI only for Email and Apple. Apple credentials and redirect URL allowlists remain unverified. | Low | 3 | — | Open | In Supabase Authentication → Providers, confirm Email and Apple are enabled; either disable Google if it is not intentionally supported or document and test its login flow. In URL Configuration, confirm there are no wildcard redirect URLs. In Apple Developer and Supabase, verify the Services ID, Team ID, Key ID, private key, and callback URL. Test Apple authentication in a Clover development or TestFlight build, not Expo Go. | Local configuration verified 2026-06-10: bundle ID `com.chloeguo.clover`, `usesAppleSignIn: true`, Apple Expo plugin present, and the app calls `signInWithIdToken({ provider: 'apple' })`. An Expo Go test returned audience `host.exp.Exponent`, which is expected for Expo Go but invalid for Clover's native Supabase client ID. Apple controls are now hidden in Expo Go. Dashboard credentials remain unverified. |

---

## Phase 4 — Technical Debt

| ID | Finding | Severity | Phase | Owner | Status | Verification Method | Notes |
|---|---|---|---|---|---|---|---|
| A-09 | RPC security fixes (`assert_authenticated_user` guard on `fetch_match_previews`, `get_unread_count`, `mark_chat_read`, `respond_to_friend_request`) were applied directly in the dashboard on 2026-06-07 and had no corresponding migration file. | Low | 4 | Codex | Fixed | Apply `supabase/020_record_rpc_auth_guards.sql` to environments missing the guarded definitions. Verify with `pg_get_functiondef`, then confirm only `authenticated` has execute permission. | Added migration 020 on 2026-06-10 with the identity helper, guarded definitions for all four RPCs, explicit `search_path`, and authenticated-only grants. App call signatures and normal behavior are unchanged. Production already had the guards; applying this migration remains a deployment step for environment parity. |
| A-10 | Three tables existed in production with no migration file: `intents` (parallel intent system, 0 rows), `invites` (invite-code system, 0 rows), `cowork_sessions` (two-user session confirmation, 0 rows). None are referenced in app code. | Low | 4 | Codex | Fixed | Apply `supabase/021_remove_unused_prototype_tables.sql`, then confirm the three names are absent from `information_schema.tables`. The migration must abort rather than drop anything if any table contains a row or has an undisclosed external dependency. | Fixed in repository 2026-06-10. Added a guarded, idempotent cleanup migration for the abandoned prototypes. It checks all three tables for data before making changes and omits `CASCADE`, preventing silent data loss or removal of unknown dependent objects. Associated unused enum types remain tracked separately under A-13. Live Supabase execution remains required. |
| A-11 | Three dashboard-only `profiles` columns existed without migration source: `location` (unused PostGIS geography), `expo_push_token` (redundant with `push_tokens`), and `one_liner` (superseded by canonical `tagline`). | Low | 4 | Codex | Fixed | Apply `supabase/022_remove_legacy_profile_columns.sql`, then confirm the three columns and their indexes are absent. Verify legacy `one_liner` values filled only empty `tagline` fields and legacy push tokens were copied only for users without a canonical `push_tokens` row. The migration must abort if `location` contains data. | Fixed in repository 2026-06-10. Added a guarded migration that preserves legacy About text and push tokens before dropping the redundant columns. Existing canonical tagline and push-token values win on conflict. Non-null PostGIS location data blocks the migration to prevent silent coordinate loss. No app code changed; live Supabase execution remains required. |
| A-12 | `update_updated_at()` and the `profiles_updated_at` trigger existed only in the dashboard, so `profiles.updated_at` maintenance was not reproducible from migrations. | Low | 4 | Codex | Fixed | Apply `supabase/023_record_profile_updated_at_trigger.sql`. Update a disposable profile without supplying `updated_at`, then confirm the value advances. Verify `pg_get_functiondef` and `pg_get_triggerdef` show the version-controlled definitions. | Fixed in repository 2026-06-10. Added an idempotent server-side timestamp trigger with an explicit safe `search_path`. The trigger always assigns database time before a profile update, matching the existing live behavior and covering writes that omit the timestamp. Live Supabase execution remains required. |
| A-13 | Three dashboard-created enum types remained without migration ownership: `friendship_status`, `location_pref`, and `time_pref`. Their former consumers were replaced or removed under A-04 and A-10. | Low | 4 | Codex | Fixed | Apply `supabase/024_remove_unused_enum_types.sql`, then confirm the three names are absent from `pg_type`. The migration must fail if any undiscovered live object still depends on an enum. | Fixed in repository 2026-06-10. Added an idempotent cleanup migration using PostgreSQL's default `RESTRICT` behavior and no `CASCADE`. `friendship_status` is obsolete after conversion to checked TEXT; `location_pref` and `time_pref` belonged to the removed `intents` prototype. Live Supabase execution remains required and depends on migrations 017 and 021 having completed first. |
| A-14 | Production had redundant uniqueness enforcement on `matches` and `friendships`. The two `matches` constraints covered the same ordered pair; friendships had a weaker ordered-pair constraint plus the canonical unordered `LEAST/GREATEST` unique index. | Low | 4 | Codex | Fixed | Confirm `matches_user_pair_unique` and `friendships_user_id_friend_id_key` are absent while `matches_user_1_id_user_2_id_key` and `idx_friendships_pair_unique` remain. Verify duplicate and reverse-direction friendship inserts are rejected. | Revised migration 025 was successfully applied to production on 2026-06-10 after live definitions confirmed the retained indexes enforce equal or stronger uniqueness. |
| A-15 | `friendships` was enabled in `supabase_realtime` without an in-app subscriber. The original audit also listed `group_sessions`, `session_events`, and `sessions`, but source review confirmed all three have active subscribers and are required. | Low | 4 | Codex | Fixed | Apply `supabase/026_remove_unused_friendships_realtime.sql`, then confirm `friendships` is absent from `pg_publication_tables` while `group_sessions`, `session_events`, and `sessions` remain. Manually verify friend-request lists still refresh when reopening or refocusing the Friends screen. | Fixed in repository 2026-06-10. Added an idempotent migration that removes only `friendships` from Realtime. Session and group-session publications are intentionally preserved because `sessionService.ts` and `groupChatsService.ts` subscribe to them. Live Supabase execution remains required. |

---

## Closed

| ID | Finding | Severity | Fixed | Verified | Notes |
|---|---|---|---|---|---|
| A-16 | `fetch_match_previews(p_user_id)` accepted a caller-supplied user ID. Any authenticated user could read another user's full match list and message previews. | Critical | 2026-06-07 | 2026-06-07 | Live function now uses `auth.uid()` throughout all CTEs. Parameter retained for app compatibility. Confirmed via `pg_get_functiondef`. |
| A-17 | `get_unread_count(p_user_id)` inherited the same caller-ID issue via delegation to `fetch_match_previews`. | Critical | 2026-06-07 | 2026-06-07 | Live function now calls `fetch_match_previews(auth.uid())`. Confirmed via `pg_get_functiondef`. |
| A-18 | `respond_to_friend_request(p_friendship_id, p_user_id, p_response)` did not verify `auth.uid() = p_user_id`. A requester could force-accept their own outbound friend request, creating an unsolicited match. | High | 2026-06-07 | 2026-06-07 | Guard `IF p_user_id IS DISTINCT FROM auth.uid() THEN RAISE EXCEPTION 'Access denied'` added as first statement. Confirmed via `pg_get_functiondef`. |
| A-19 | `mark_chat_read(p_match_id, p_user_id)` accepted a caller-supplied user ID. When chained with A-16, an attacker could zero another user's unread badge. | Low | 2026-06-07 | 2026-06-07 | Same guard added as first statement. Confirmed via `pg_get_functiondef`. |
